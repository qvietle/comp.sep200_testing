# comp.sep200_testing
Test cases for Software Testing assignment

## To run tests:
- In the terminal run "npm test 'test path'"
  - Without any of the quotation marks of course
- Example:
  - Assuming your terminal's working directory is the root directory.
  - npm test ./tests/example.test.js 
- Alternatively Run All tests in the tests directory with:
  - npm test
